迁移采集表详情为独立页面，并将若干弹窗改为路由驱动的 Tab（2026-05-31）

Why:
- 解决弹窗层级复杂、不能深链（分享/书签）的问题。
- 提升日志/模板/字段对比等大内容的阅读与交互体验（更大空间、按需加载、缓存支持）。

What (本次改动概述):
- 新增/修改路由与页面：
  - 新增 `frontend/src/views/table/detail.vue`（详情页，使用 v-tabs: info/logs/job/fields/result/history）。
- 组件迁移与重构（移除对话框 props，改为路由驱动）：
  - 修改 `frontend/src/components/table/TableDetail.vue`：从 dialog 模式改为详情页表单（通过 route.params.tid 拉取并保存），移除了变更历史 dialog；关闭按钮改为 router.back()。
  - 修改 `frontend/src/components/table/LogFiles.vue`：左右两栏布局，左侧列出日志列表，右侧显示选中日志内容（保留复制、下载、加载与空态逻辑）。
  - 修改 `frontend/src/components/table/FieldsCompare.vue`、`AddaxJob.vue`、`AddaxResult.vue`：移除 dialog props，改为从 route.params.tid 拉数据并渲染。
  - 新增 `frontend/src/components/table/ChangeHistory.vue`：把原先 TableDetail 内的变更历史弹窗抽为独立组件并在 detail 页面作为 history tab 渲染。
- 列表页适配：
  - 修改 `frontend/src/views/table/index.vue`：action 列的“更多”入口改为直接导航到详情页（`/table/detail/:tid?tab=...`），保留常用快速按钮（采集/更新/删除/批量修改）。
- 其他小修复：
  - 调整 `frontend/src/layouts/default/Topbar.vue`、`frontend/src/views/table/batch-add.vue` 的小型类型/未使用变量问题，以让类型检查通过。

Impact（影响/兼容性）：
- 行为变更：以前通过弹窗查看详情/日志/模板等的操作，现在会导航到详情页（有 deep-link 优势，但用户的操作流从 Modal 切换为 Page）。
- 后端接口未变，本次改动主要是前端路由与组件重构。

Test（已验证）:
- 运行 `yarn build:frontend`（包含 `vue-tsc` 类型检查 + `vite build`），构建与类型检查通过。
- 手动验证要点（建议在本地验证）：列表页跳转到详情页、tabs 切换、日志加载/复制/下载、AddaxJob 编辑保存、字段对比加载、变更历史分页与筛选、批量修改对话框仍可用。

Follow-ups / TODO：
- 可选：把 `BatchUpdate` 也迁移为页面以统一交互风格（目前仍为 dialog）。
- 可选：在详情页 tabs 上显示 badge（如日志条数、失败数、历史条数）。
- 性能：日志与大体量文本建议按需分页或后端提供 tail/区间加载接口以优化大日志处理。

Files changed (主要文件列表)：
- added: frontend/src/views/table/detail.vue
- added: frontend/src/components/table/ChangeHistory.vue
- modified: frontend/src/components/table/TableDetail.vue
- modified: frontend/src/components/table/LogFiles.vue
- modified: frontend/src/components/table/FieldsCompare.vue
- modified: frontend/src/components/table/AddaxJob.vue
- modified: frontend/src/components/table/AddaxResult.vue
- modified: frontend/src/views/table/index.vue
- modified: frontend/src/views/table/batch-add.vue (小调整)
- modified: frontend/src/layouts/default/Topbar.vue (类型声明修复)

验证状态：
- Frontend build & type-check: PASS (`yarn build:frontend` ran successfully at commit time)

Notes:
- 本次变更以小步迁移、逐个组件改造的策略执行。若需要回退到旧的 modal 行为，需要额外实现 route-driven modal wrapper 或保留一份 dialog 兼容层。

----
时间：2026-05-31
作者：自动化提交（AI 助手）
